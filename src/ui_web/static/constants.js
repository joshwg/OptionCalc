/* OptionCalculator web UI — constants */

'use strict';

// ── Timing (ms) ───────────────────────────────────────────────────────────────

/** How long the "Saved" confirmation stays visible */
const SAVED_MSG_DISMISS_MS   = 2000;

/** Debounce delay for ticker symbol search */
const TICKER_SEARCH_DELAY_MS = 400;

/** Delay before redirecting after session expiry notification */
const SESSION_EXPIRED_REDIRECT_MS = 5000;
