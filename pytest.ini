[pytest]
testpaths = src/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    network: marks tests that require live network access (deselect with -m "not network")
    slow: marks tests that are slow (binomial with many steps)
